# ls
alias ll="ls -Glrth"
alias ls="ls -G"

alias tns="tmux new -s"
alias ta="tmux attach"
alias tat="tmux attach -t"
alias tks="tmux kill-session -t"

# git
alias g="git"

# grep
alias grep="grep --color=auto --exclude-dir=.mypy_cache"

# vim
alias vim="$(which nvim)"

# peco
alias peco="peco --exec \"xclip -selection c\""
